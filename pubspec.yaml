name: travel_planner
description: A travel planner application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  uuid: ^3.0.4
  fl_chart: ^0.70.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

